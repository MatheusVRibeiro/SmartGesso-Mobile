# SmartGesso Mobile — Regras Hermes

## ANTES DE QUALQUER TAREFA

**Carregue skills relevantes SEMPRE.** Skills obrigatórias:
- `spec-a23` — workflow de implementação
- `react-native-expo-scaffold` — scaffolding e estrutura
- `codebase-health-check` — antes de finalizar
- `frontend-security-audit` — análise de segurança mobile/frontend

## Stack
- React Native + Expo
- TypeScript strict
- React Navigation
- Axios + JWT refresh (via API BFF)
- SecureStore para tokens

## Regras
- Autenticação via BFF (API na porta 3000) — nunca expor JWT diretamente
- Usar companyId da autenticação/contexto, nunca do body
- Não colocar secrets no código mobile
- Executar lint, typecheck e build antes de concluir

## API
- Backend: NestJS+Prisma+MySQL (212.85.3.212:3306)
- Auth: BFF no Admin Web (porta 3001)

## Status
- Projeto vazio — começar do zero com Expo
